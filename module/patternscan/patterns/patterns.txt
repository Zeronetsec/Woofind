/dev/tcp/
/etc/passwd
/etc/shadow
/root/.ssh
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
Authorization: Bearer
BEARER_TOKEN
BEGIN OPENSSH PRIVATE KEY
BEGIN PGP PRIVATE KEY BLOCK
GITHUB_TOKEN
JWT_SECRET
WScript.Shell
\x00\x00\x00
archive/zip
bash -i
cmd.Output()
cmd.Run()
cmd.Start()
cmd.exe /c
compress/gzip
crontab -e
crypto/aes
crypto/cipher
crypto/des
crypto/rc4
curl -X POST
curl -s
encoding/gob
html/template
http.Client
http.Post(
id_rsa
io.ReadAll(res.Body)
ioutil.WriteFile
mongodb://
mysql://
nc -e
ncat -e
net.Dial(
net.Listen(
os.OpenFile
os.Rename
os.Setenv
os.WriteFile
postgres://
powershell.exe -c
reflect.ValueOf
requests.get(
rot13
shred -u
sql.Open(
syscall.Exec
syscall.ForkExec
text/template
unsafe.Pointer
urllib.request
wget -q
x-api-key
~/.ssh/authorized_keys
